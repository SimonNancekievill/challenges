info:
  name: Get bookmark by ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/bookmarks/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

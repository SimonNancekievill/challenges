info:
  name: Get One Thread
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/threads/:id
  params:
    - name: id
      value: "30"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Create one Thread
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3000/threads
  body:
    type: json
    data: |-
      {
        "title": "Thread",
        "author": "Simon",
        "body": "lorem ipsum"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

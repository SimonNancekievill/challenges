info:
  name: Delete Comment Body
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseURL}}/comments/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
